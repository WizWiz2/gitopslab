# Справочник Скриптов

Полное описание всех скриптов платформы GitOpsLab.

---

## 📁 Структура

```
gitopslab/
├── start.bat                    # Главный запуск
├── stop.bat                     # Остановка
├── restart.bat                  # Перезапуск
├── health-check.bat             # Проверка здоровья (Windows)
├── run-e2e.bat                  # E2E тесты
└── scripts/
    ├── bootstrap.sh             # Мастер-оркестратор
    ├── init-gitea.sh            # Инициализация Gitea
    ├── init-woodpecker.sh       # Настройка Woodpecker
    ├── init-argocd.sh           # Настройка Argo CD
    ├── pre-provision-oauth.sh   # Pre-provisioning OAuth
    ├── health-check.sh          # Проверки здоровья (bash)
    ├── collect-creds.sh         # Сбор учётных данных
    ├── wait-for.sh              # Утилита ожидания
    ├── wipe.sh                  # Полная очистка
    ├── dashboard-token.sh       # Токен Dashboard
    └── update-gateway-ip.sh     # Обновление gateway IP
```

---

## Windows-скрипты (.bat)

### 🚀 start.bat — Главный запуск платформы

**Назначение:** Запускает всю платформу GitOpsLab на Windows.

**Вызов:**
```batch
start.bat
```

**7 фаз запуска:**

| Фаза | Название | Действия |
|------|----------|----------|
| 0 | Environment Check | Проверка Podman, `.env`, SSH tools |
| 1 | Podman Machine | Создание/запуск Podman VM |
| 2 | Verify Podman API | Проверка работоспособности API |
| 3 | Configure Machine | DNS, gateway IP, insecure registries |
| 4 | Create Network | Создание сети `gitopslab` |
| 5 | Start Services | Запуск Gitea, Woodpecker, Bootstrap |
| 6 | MLflow Image | Сборка и push MLflow образа |
| 7 | Platform Bootstrap | Ожидание завершения bootstrap |

**Зависимости:** Podman Desktop, WSL2

---

### 🛑 stop.bat — Остановка платформы

**Назначение:** Останавливает все контейнеры и очищает ресурсы.

**Вызов:**
```batch
stop.bat          # Мягкая остановка (volumes сохраняются)
stop.bat --clean  # Полная очистка (всё удаляется)
stop.bat -c       # Сокращённая форма
```

**Режимы:**

| Режим | Контейнеры | k3d кластер | Volumes | Сети |
|-------|------------|-------------|---------|------|
| Мягкий | ❌ Удаляет | ❌ Удаляет | ✅ Сохраняет | ✅ Сохраняет |
| Clean | ❌ Удаляет | ❌ Удаляет | ❌ Удаляет | ❌ Удаляет |

---

### 🔄 restart.bat — Перезапуск

**Назначение:** Полный перезапуск с очисткой.

**Вызов:**
```batch
restart.bat
```

**Что делает:**
1. `stop.bat --clean`
2. Ожидание 5 сек
3. `start.bat`

---

### 🏥 health-check.bat — Проверка здоровья

**Назначение:** Запуск проверок здоровья платформы.

**Вызов:**
```batch
health-check.bat smoke  # Быстрая проверка
health-check.bat full   # Полная проверка
```

**Подробнее:** См. [HEALTH_CHECKS.md](HEALTH_CHECKS.md)

---

### 🧪 run-e2e.bat — E2E тесты

**Назначение:** Запуск end-to-end тестов.

**Вызов:**
```batch
run-e2e.bat
```

---

## Shell-скрипты (scripts/)

### 🎯 bootstrap.sh — Мастер-оркестратор

**Назначение:** Создаёт k3d кластер, устанавливает ArgoCD, инициализирует все сервисы.

**Вызов:** Автоматически из контейнера `platform-bootstrap`

**Зависимости:** Docker API, k3d CLI, kubectl

**Что делает:**
1. `create_registry()` — Создаёт k3d registry на порту 5002
2. `create_cluster()` — Создаёт k3d кластер с port mappings
3. `ensure_loadbalancer_config()` — Конфигурирует load balancer
4. `patch_registry_hosts()` — Патчит /etc/hosts в k3d нодах
5. `ensure_kubeconfig()` — Генерирует kubeconfig
6. `wait_for_k8s_api()` — Ждёт готовности K8s API
7. `install_argocd()` — Устанавливает Argo CD
8. Вызывает `init-gitea.sh`, `init-woodpecker.sh`, `init-argocd.sh`
9. Применяет root application

**Переменные окружения:**

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `K3D_CLUSTER_NAME` | `gitopslab` | Имя кластера |
| `K3D_API_PORT` | `6550` | Порт K8s API |
| `K3D_REGISTRY_NAME` | `registry.localhost` | Имя registry |
| `K3D_REGISTRY_PORT` | `5000` | Внутренний порт registry |
| `ARGOCD_VERSION` | `2.10.7` | Версия Argo CD |

---

### 📦 init-gitea.sh — Инициализация Gitea

**Назначение:** Создаёт admin-пользователя, репозиторий `platform`, push кода, токены.

**Вызов:** Из `bootstrap.sh`

**Зависимости:** Gitea должна быть запущена

**Что делает:**
1. Создаёт admin-пользователя через `gitea admin user create`
2. Создаёт репозиторий `platform` через API
3. Клонирует репо, синхронизирует код из `/workspace`
4. Создаёт CI/CD токен для Argo и Woodpecker
5. Сохраняет токен в `/workspace/.gitea_token`

**Переменные окружения:**

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `GITEA_INTERNAL_URL` | `http://gitea:3000` | URL Gitea |
| `GITEA_ADMIN_USER` | `gitops` | Имя администратора |
| `GITEA_ADMIN_PASSWORD` | `gitops1234` | Пароль администратора |

---

### 🪵 init-woodpecker.sh — Настройка CI

**Назначение:** Активирует репозиторий в Woodpecker, создаёт секреты, фиксит webhook.

**Вызов:** Из `bootstrap.sh`

**Зависимости:** Woodpecker Server, пользователь должен залогиниться (см. INSTALLATION.md)

**Что делает:**
1. Генерирует JWT-токен для Woodpecker API
2. Активирует репозиторий `platform`
3. Устанавливает trusted флаги
4. Фиксит URL webhook'а на внутренний адрес
5. Создаёт секреты `gitea_user` и `gitea_token`

> [!WARNING]
> Если пользователь не залогинился в Woodpecker через браузер, скрипт выводит сообщение о необходимости ручного шага и завершается.

---

### 🔧 init-argocd.sh — Настройка GitOps

**Назначение:** Патчит Argo CD, добавляет репозиторий, применяет root app.

**Вызов:** Из `bootstrap.sh`

**Зависимости:** ArgoCD pods должны быть Ready

**Что делает:**
1. Ждёт готовности всех pods в namespace `argocd`
2. Патчит deployment для режима `--insecure` (HTTP)
3. Патчит service на NodePort `30081`
4. Создаёт Secret с credentials репозитория
5. Применяет `root-app.yaml`

---

### 🔑 pre-provision-oauth.sh — Pre-provisioning OAuth

**Назначение:** Создаёт OAuth-приложение в Gitea ДО запуска Woodpecker.

**Вызов:** Из `start.bat` перед запуском Woodpecker

**Что делает:**
1. Проверяет существующие credentials в `.env`
2. Удаляет старое OAuth-приложение (если есть)
3. Создаёт новое OAuth-приложение `woodpecker`
4. Записывает `WOODPECKER_GITEA_CLIENT` и `WOODPECKER_GITEA_SECRET` в `.env`

---

### 🏥 health-check.sh — Проверки здоровья (bash)

**Назначение:** 7 категорий проверок здоровья системы.

**Вызов:** Из контейнера bootstrap или вручную

**Категории проверок:**

| # | Категория | Что проверяет |
|---|-----------|--------------|
| 1 | Network | k3d network, gateway IP, Docker API |
| 2 | Registry | Доступность registry, контейнер |
| 3 | Service Connectivity | gitea:3000, woodpecker:8000/9000 |
| 4 | OAuth Config | WOODPECKER_GITEA_CLIENT/SECRET |
| 5 | Woodpecker DB | Наличие пользователя в БД |
| 6 | File Consistency | DOCKER_HOST в .woodpecker.yml |
| 7 | k3d Cluster | Кластер, kubectl, ArgoCD pods |

---

### 📋 collect-creds.sh — Сбор учётных данных

**Назначение:** Собирает все credentials в один файл.

**Вызов:**
```bash
bash scripts/collect-creds.sh > credentials.txt
```

**Выводит:** INI-формат с секциями [Gitea], [Woodpecker], [ArgoCD], [MLflow], [Dashboard], [MinIO], [KubeAPI]

---

### ⏳ wait-for.sh — Утилита ожидания

**Назначение:** Ждёт доступности HTTP-эндпоинта.

**Вызов:**
```bash
bash scripts/wait-for.sh "http://gitea:3000/api/v1/version"
```

**Параметры:**
- Timeout: 300 секунд
- Интервал проверки: 5 секунд

---

### 🧹 wipe.sh — Полная очистка

**Назначение:** Полная очистка окружения (Linux/Mac).

**Вызов:**
```bash
bash scripts/wipe.sh
```

**Что делает:**
1. `docker compose down -v`
2. Удаляет `.gitea_token`
3. `k3d cluster delete gitopslab`

---

### 🎫 dashboard-token.sh — Токен Dashboard

**Назначение:** Генерирует токен для K8s Dashboard.

**Вызов:**
```bash
bash scripts/dashboard-token.sh
```

**Что делает:**
1. Создаёт ServiceAccount `admin-user`
2. Создаёт ClusterRoleBinding
3. Генерирует и выводит токен

---

### 🌐 update-gateway-ip.sh — Обновление Gateway IP

**Назначение:** Обновляет HOST_GATEWAY_IP в конфигурационных файлах.

**Вызов:**
```bash
bash scripts/update-gateway-ip.sh
```

---

## 🔗 Связанная Документация

- [ARCHITECTURE.md](ARCHITECTURE.md) — Архитектура системы
- [CONFIGURATION.md](CONFIGURATION.md) — Гид по конфигурации
- [INSTALLATION.md](INSTALLATION.md) — Инструкции по установке
