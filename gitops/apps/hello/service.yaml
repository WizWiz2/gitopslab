apiVersion: v1
kind: Service
metadata:
  name: hello-api
  labels:
    app: hello-api
spec:
  type: LoadBalancer
  selector:
    app: hello-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
